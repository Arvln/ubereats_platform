import { TChannelItem } from "types/features";
import { TShop } from 'types/shop';

export type Prop = {
	data: TChannelItem[][];
	size: number;
	offset: number;
}

export type {
	TChannelItem,
	TShop
}
