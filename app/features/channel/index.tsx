import {
	wrapper,
} from 'styles/features/Channel.module.scss';

function Channel() {
	return (
		<div className={ wrapper }>
			<div style={{ backgroundColor: 'skyblue', paddingBottom: '100%' }} />
		</div>
	)
}

export default Channel;
