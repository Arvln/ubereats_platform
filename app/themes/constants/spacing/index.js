const { spacing: headerSpacing } = require('../../features/header');
const { spacing: shortcutSpacing } = require('../../features/shortcut');

module.exports = {
	...headerSpacing,
	...shortcutSpacing
}
