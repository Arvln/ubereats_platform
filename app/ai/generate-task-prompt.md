Read the following files in full before writing anything:

- `ai/{migration}/objective.md`
- `ai/{migration}/prompt.md`
- `ai/task-generation-rules.md`

Replace `{migration}` with the target migration folder name before running.

Then generate `ai/{migration}/task.md` following the rules and output format defined in those files.

Do not begin writing until you have read all three files.
