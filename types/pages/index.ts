import { TShortcut } from '../shortcut';
import { TCarousel } from '../carousel';
import {
	TChannelData
} from '../channel';

export type Prop = {
	shortcut: TShortcut[],
	carousel: TCarousel[],
	channel: TChannelData[]
}
