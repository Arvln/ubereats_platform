import { TShortcut } from '../features';
import { TCarousel } from '../features';
import {
	TChannelData
} from '../features';

export type Prop = {
	shortcut: TShortcut[],
	carousel: TCarousel[],
	channel: TChannelData[]
}
